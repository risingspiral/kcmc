describe('A filter', function() {
    it('emits an event when text is typed into it', function() {});
    it('can be cleared', function() {});
    it('emits an event when it is cleared.', function() {});
    it('shows a text field and a reset button', function(){});
});